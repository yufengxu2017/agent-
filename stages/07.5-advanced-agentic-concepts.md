# Stage 7.5 — 進階 Agentic Workflow 概念（Advanced Agentic Concepts，骨幹 + reading list）

> **繁體中文** | (zh-Hans / en mirror defer 中)

⏱ **時間估算**：1 週（約 5 小時——不寫 code、只讀資源建 mental model）

> 💡 這是一份 **map + reading list**，不是完整教學。Stage 4 / 6 / 7 學完已能做 production agent；本 stage 告訴你**還有哪些 frontier 概念存在**、**該讀哪些 paper / blog**，避免你在工作上踩到別人已踩過的坑。

> 📋 **本章組成**：為什麼有這 stage → **核心 mental model：Types → Config → Repo → Service 四層工作邊界**（diagram）→ 12 個進階概念 skeleton → 為什麼選這 12 個 → **OpenAI Harness Engineering 5 原則（cross-cutting）** → 完整 reading path → 自我檢查

## 🎯 為什麼有這 stage

Stage 4 教你挑 framework、Stage 6 教 context engineering、Stage 7 教 harness + production 化——這 3 個 stage 加起來足以做 **70% 的 production agent**。

但 frontier lab（Anthropic / OpenAI / Cognition / Microsoft）跟學術界（Stanford / CMU / Princeton）2024-2026 推出的 12+ 個進階概念，**有些你需要知道它存在、即使現在不用**。本 stage 是這些概念的 map：

- 不是要你全學會
- 不是要你全用
- 是讓你**遇到問題時知道該翻哪份 paper / blog**
- 是讓你**評估別人寫的 agent 時，能說出「他在做 plan-act-reflect loop」而不只是「他在 retry」**

## 🧭 核心 mental model：四層工作邊界（work boundary）

進階 agentic workflow 的關鍵 mental model 是**工作邊界**——agent 操作的對象屬於 stack 哪一層？跨層越界會出什麼問題？

> ⚠️ **這套 4 層跟 Stage 7 的 prompt → context → harness 三層不一樣、是兩種不同視角**：
> - **Prompt → Context → Harness**（Stage 7）：**stack 位置**——你正在工程「字串 / 資訊 / 外圍 runtime」哪一個對象？
> - **Types → Config → Repo → Service**（本 stage）：**自主權範圍**——agent 能動到 stack 多深？跨層越界算違規嗎？
>
> 兩者**正交**、解不同問題。讀完本節後可以同時用兩種視角看 agent 系統。

借用軟體架構的 **Types → Config → Repo → Service** 分層、套到 agent 系統：

```
┌────────────────────────────────────────────────────────────────┐
│ Service layer ─────────────── 業務邏輯 / 流程編排 │
│ • Multi-agent supervisor / planner-executor │
│ • Plan-Act-Reflect loops (Reflexion / Self-Refine) │
│ • Agent-as-Judge / Constitutional review │
│ • Self-organizing teams │
├────────────────────────────────────────────────────────────────┤
│ ── Work boundary 3 ── │
├────────────────────────────────────────────────────────────────┤
│ Repo layer ──────────────────── 狀態 / 記憶 / 知識 │
│ • Shared memory (.coord/memory.yml) │
│ • Vector DB / RAG retrieval │
│ • Episodic memory store (Reflexion) │
├────────────────────────────────────────────────────────────────┤
│ ── Work boundary 2 ── │
├────────────────────────────────────────────────────────────────┤
│ Config layer ─────────────────── 參數 / 政策 │
│ • plan.yml / context_policy / budget │
│ • Acceptance presets / banned phrasing │
│ • Autonomy gradient (suggest / propose / execute) │
├────────────────────────────────────────────────────────────────┤
│ ── Work boundary 1 ── │
├────────────────────────────────────────────────────────────────┤
│ Types layer ─────────────────── 契約 / Schema │
│ • Task brief format / result.json schema │
│ • Tool definitions / MCP primitives │
│ • Prompt template signatures │
└────────────────────────────────────────────────────────────────┘
```

→ **每一層上下都是一個「工作邊界」**。agent 操作的範圍 = 它的自主權範圍：

- **Agent at Types layer** = 只能 fit 既有契約、不能改 schema（例：codex 接 brief 加 gloss）
- **Agent at Config layer** = 可以調 budget / policy 但不能改 memory（例：context-budget 改 max_cost_usd）
- **Agent at Repo layer** = 可以讀寫 memory / vector store 但不能 redesign workflow
- **Agent at Service layer** = 可以 recompose 整個 workflow、最高自主權

### 為什麼這個 mental model 重要

[`agent-collab-skills` v0.2.1 的 F11/F12 incident](https://github.com/WenyuChiou/agent-collab-skills/blob/main/docs/observed-failure-modes.md) 就是 work boundary 失守的活教材：

- **F11**：Codex 在 Types layer 該做 sweep（`「默認」→「預設」`），卻爬到 Repo layer 改了 `style-guide.md` 的 documentation table——**跨 boundary 違規**
- **F12**：Codex 同樣被指派 Types layer 工作（加 inline gloss），卻擅自加 attribution lines 到 Service layer 內容——**跨 boundary 越權**

→ 設計 agent 工作時、**先想清楚這個 task 在哪一層、能跨幾層、不能跨什麼**。工作邊界寫進 brief（用 `agent-collab-skills` 的 task-splitter pre-task scope confirmation）+ enforce 在 acceptance gate。

## 📚 12 個進階概念 — skeleton

每個概念 ≤ 4 行：一句話定義 + stack 哪一層 + 最值得讀的 1 個資源。

### 1. Work Boundary / Scope Discipline
- 哪一層：跨所有層（discipline）
- 定義：agent 只動 brief 指定的對象，不越界
- 最佳讀物：[Hamel Husain — Evals + Skills](https://hamel.dev/blog/posts/evals-skills/) / `agent-collab-skills` F11/F12

### 2. Contract-driven Hand-offs
- 哪一層：Types + Service（contract 在 types、執行在 service）
- 定義：上游 agent 承諾的 artifacts、下游 agent 必須驗證收到
- 最佳讀物：[Anthropic — Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) Routing pattern

### 3. Speculative / Parallel Exploration
- 哪一層：Service（編排）
- 定義：跑 N 條 alternative 路徑、取最佳那條（不只獨立 parallel）
- 最佳讀物：[LangGraph Plan-Execute Tutorial](https://blog.langchain.com/planning-agents/)

### 4. Agent-as-Judge / Constitutional AI
- 哪一層：Service（agent 評 agent）
- 定義：用一個 agent 評另一個 agent 的輸出、依 principles 反覆修正
- 最佳讀物：[Constitutional AI (Bai 2022)](https://arxiv.org/abs/2212.08073)

### 5. Plan-Act-Reflect Loop
- 哪一層：Service（單 agent 自我循環）
- 定義：write plan → execute → critique → revise → re-execute，直到 PASS 或 EXHAUSTED
- 最佳讀物：[Reflexion (Shinn 2023)](https://arxiv.org/abs/2303.11366) + [Self-Discover (Zhou ICML 2024)](https://arxiv.org/abs/2402.03620)

### 6. Hierarchical Task Decomposition
- 哪一層：Service（多層 supervisor）
- 定義：supervisor → worker → sub-worker、≥ 2 層 recursion
- 最佳讀物：[Microsoft AutoGen GroupChat docs](https://microsoft.github.io/autogen/)

### 7. Autonomy Gradients / Trust Layers
- 哪一層：Config（autonomy policy）
- 定義：agent 不同任務有不同自主權（suggest / propose / execute）
- 最佳讀物：[Claude Code permission system](https://docs.claude.com/en/docs/agents-and-tools/claude-code/overview)

### 8. Cost-aware Budget Gates
- 哪一層：Config（cost policy）
- 定義：超 \$ 預算自動停 / 升級審核（不只 token 上限）
- 最佳讀物：[OpenAI Harness Engineering article (2026-02)](https://openai.com/index/harness-engineering)

### 9. Failure Injection / Chaos Eval
- 哪一層：Service（測 agent 容錯）
- 定義：故意給 broken input、stale data、API timeout、看 agent 怎麼處理
- 最佳讀物：[Hamel Husain — Evals blog series](https://hamel.dev/blog/posts/evals/)（適配 chaos eval）

### 10. Self-organizing Teams
- 哪一層：Service（agent 動態協商 role）
- 定義：agents 不是預先指派 role、而是依任務動態分工
- 最佳讀物：[CAMEL (Li 2023)](https://arxiv.org/abs/2303.17760) + AutoGen

### 11. Spec-driven Development
- 哪一層：Types（spec = code）
- 定義：agent 任務由 formal spec（YAML / JSON Schema）定義、不是自由 prompt
- 最佳讀物：[DSPy](https://github.com/stanfordnlp/dspy) signatures tutorial

### 12. Graceful Degradation Paths
- 哪一層：Config（fallback policy）
- 定義：frontier model 掛掉 → fallback 便宜 model + 降預期、不直接 crash
- 最佳讀物：[OpenRouter routing docs](https://openrouter.ai/docs) + [Anthropic model fallback](https://docs.claude.com/en/docs/build-with-claude/models)

## 為什麼選這 12 個

- 都跟 `agent-collab-skills` v0.2.1 / v0.2.2 直接 map 得到（不是學術綱要）
- 都有可驗證 primary source（不是空談）
- 都在 Stage 4 / 6 / 7 已覆蓋的概念之外（沒重複）
- 避免「無限延伸」——其他進階概念（Voyager skill learning / MemoryLLM / world models）很重要但**先學這 12 個再說**

## 🔬 OpenAI Harness Engineering 5 原則（2026-02、cross-cutting）

2026-02 OpenAI 發佈 [Harness Engineering](https://openai.com/index/harness-engineering/) 文章、整理出 5 個**跨所有 8 元件**的設計原則。**12 個進階概念是「該學什麼」、這 5 個原則是「怎麼把它們做對」**——下面每個原則對到我們現有的 skill / tool。

### 為什麼要在意這些原則 — Why → What → How 概念圖

下圖以「**痛點（Why）→ 原則（What）→ 實作（How）**」三層解釋這些原則在解什麼問題、用什麼工具落地。

```
                            Why → What → How
                        ════════════════════════
        問題 (Why 痛點)              原則 (What)              實作 (How 工具)

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ Context 200k 滿 │         │ Progressive     │         │ Skills references/ │
  │ Multi-agent     │ ───────▶│ Disclosure +    │ ───────▶│ CLAUDE.md @-import │
  │ context overflow│         │ Memory persist  │         │ .ai/<task> brief   │
  └─────────────────┘         └─────────────────┘         └────────────────────┘

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ Agent 看不懂    │         │ Legibility +    │         │ AGENTS.md 100 行   │
  │ 自家 codebase   │ ───────▶│ Tool Doc / ACI  │ ───────▶│ poka-yoke 工具設計 │
  │                 │         │                 │         │ 一致 schema 命名   │
  └─────────────────┘         └─────────────────┘         └────────────────────┘

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ 多 agent desync │         │ System of       │         │ docs/ + .coord/    │
  │ 不同事實版本    │ ───────▶│ Record          │ ───────▶│ shared-memory      │
  │                 │         │                 │         │  skill             │
  └─────────────────┘         └─────────────────┘         └────────────────────┘

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ 隨機 drift /    │         │ Taste Invariants│         │ acceptance-gate    │
  │ Review 漏抓     │ ───────▶│ + Transparency  │ ───────▶│ preset YAMLs       │
  │                 │         │ (planning 顯示) │         │ evaluator-optimizer│
  └─────────────────┘         └─────────────────┘         └────────────────────┘

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ Agent 寫 PR 快  │         │ Throughput      │         │ mandatory preset   │
  │ Human QA 跟不上 │ ───────▶│ Changes Merge   │ ───────▶│ + LLM-as-judge     │
  │                 │         │ Philosophy      │         │ + Human spot-check │
  └─────────────────┘         └─────────────────┘         └────────────────────┘

  ┌─────────────────┐         ┌─────────────────┐         ┌────────────────────┐
  │ 一上來就堆      │         │ Simplicity      │         │ 先 basic LLM call  │
  │ multi-agent     │ ───────▶│ (Anthropic)     │ ───────▶│ 確認需要才加 agent │
  │ overkill        │         │                 │         │                    │
  └─────────────────┘         └─────────────────┘         └────────────────────┘
```

→ **6 個痛點 → 5 + 3 個原則**（OpenAI 5 + Anthropic 3 extra）→ **8+ 個具體工具 / 機制**。
→ 下面 5 節展開 OpenAI 原則本身、最後 §Anthropic ↔ OpenAI 對照 列出兩家對照詞 + 推薦讀本。

### 1. Legibility — 讓 agent 能讀懂 codebase / docs

> "Because the repository is entirely agent-generated, it's optimized first for **Codex's legibility**." — OpenAI

方向跟「讓人讀懂 agent 輸出」相反——把 agent 當新進工程師、為它優化 navigability。

- **怎麼做**：consistent schema 命名、檔案大小限制（不讓 agent 一次讀爆）、`docs/` hierarchical 結構讓 agent 能 traverse
- **跨 work boundary**：Repo + Types
- **對應 tool**：Claude Code Skill 的 `references/` 機制 + AGENTS.md / CLAUDE.md pattern

### 2. System of Record — 知識住 docs、不住 prompt

> "The repository's knowledge base lives in a structured `docs/` directory **treated as the system of record**. A short `AGENTS.md` (roughly 100 lines) is injected into context and serves primarily as a map." — OpenAI

唯一權威來源、agent 從這裡讀寫、不要把 context 塞 system prompt。

- **怎麼做**：100 行 entry map（AGENTS.md / CLAUDE.md）pointing 到 `docs/` 深處、不在 prompt 重複內容
- **跨 work boundary**：Repo
- **對應 tool**：`.coord/memory.yml`（[agent-shared-memory](https://github.com/WenyuChiou/agent-collab-skills) skill）+ AGENTS.md / CLAUDE.md pattern

### 3. Progressive Disclosure — 從 small entry point 往深挖

> "Agents start with a small, stable entry point and **are taught where to look next**, rather than being overwhelmed up front." — OpenAI

跟 #2 SoR 一起運作：SoR 提供 destination、Progressive Disclosure 是怎麼導航。

- **怎麼做**：AGENTS.md 100 行 + 指向 `docs/subsystem-X.md`（agent 需要才讀深處）
- **跨 work boundary**：Repo + Types
- **對應 tool**：Claude Code Skill 的 `references/` 機制（只在 agent 需要才 load）+ Codex `.ai/<task>.md` brief pattern（先讀 brief 再決定深挖）

### 4. Architecture & Taste Invariants — 用 lint 強制不變量

> "We enforce these rules with custom linters and structural tests, plus a small set of **'taste invariants.'** ... **By enforcing invariants, not micromanaging implementations**, we let agents ship fast." — OpenAI

定義邊界、不細管實作。Lint 強制 structured logging、命名、檔案大小、reliability。

- **怎麼做**：custom linter / structural test + 「taste invariant」list（檔案 < 500 行、schema 命名 pattern、structured logging）
- **跨 work boundary**：Config + cross-cutting enforce 跨所有層
- **對應 tool**：`agent-acceptance-gate` YAML preset（`multi-locale-mirror-sync.yml` / `catalog-entry-add.yml` / `fact-check-frontier-models.yml`）——預先 codify「跑出來該長什麼樣」

### 5. Throughput Changes Merge Philosophy — agent 高吞吐 → 人類 QA 變瓶頸

> "...3.5 PRs per engineer per day... **the bottleneck became human QA capacity**." — OpenAI

agent 寫 code 變快了、人類 review 跟不上、必須把 QA 自動化 / 讓 agent 自我驗證。

- **怎麼做**：自動 lint + 自動 test + 自動 acceptance gate；commit 前不依賴人逐行讀
- **跨 work boundary**：Service（merge workflow）
- **對應 tool**：`agent-acceptance-gate` 整套、特別是 mandatory preset 機制（trigger fire → preset 必跑）

### 5 原則 × Stage 7 Harness 8 元件對照圖

下表展示 5 個原則怎麼作用到 [Stage 7 Harness Engineering 的 8 個核心元件](07-multi-agent-production.md#harness-的-8-個核心元件)上：

```
                              Stage 7 Harness 8 元件
                ┌───────────┬──────┬──────┬──────┬──────┬──────┬──────┬──────┐
                │ 1.Agent   │ 2.   │ 3.   │ 4.   │ 5.   │ 6.   │ 7.   │ 8.   │
                │ Loop      │ Tool │ Ctx  │ Retry│ Sand │ Obs  │ Eval │ Cost │
                │           │ Reg  │ Mgr  │      │ box  │      │      │ Lat  │
┌───────────────┼───────────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│ 1.Legibility  │           │  ✓   │  ✓   │      │      │  ✓   │      │      │
│ 2.SoR         │           │      │ ✓★   │      │      │  ✓   │      │      │
│ 3.Progr.Disc. │     ✓     │      │ ✓★   │      │      │      │      │  ✓   │
│ 4.Invariants  │           │  ✓   │      │  ✓   │  ✓   │      │ ✓★   │      │
│ 5.Merge Phil. │           │      │      │      │      │      │ ✓★   │  ✓   │
└───────────────┴───────────┴──────┴──────┴──────┴──────┴──────┴──────┴──────┘
                              ✓ = applies     ✓★ = primary lever
```

→ **Context Manager（#3）+ Eval（#7）是被 4-5 個原則同時作用的熱點**——這也是 v0.2.3 preset / `agent-acceptance-gate` / `agent-shared-memory` 都圍著這兩個元件設計的原因。

→ **Tool Registry（#2）+ Observability（#6）次熱**——被 3 個原則影響、Legibility 把 schema 寫對 + Invariants 把 lint 寫對 + SoR 把紀錄寫對。

→ **Retry / Sandbox / Cost-Latency** 只被 1-2 個原則作用——這幾個元件相對「機械」、原則上對應 1 個 lever 就夠。

### 📚 Anthropic ↔ OpenAI cross-vendor 對照 + 推薦讀本

OpenAI 那 5 原則 Anthropic 大部分也都討論過、用的詞不同。下表是 cross-vendor 對照、每個都附 canonical URL：

| OpenAI 原則 | Anthropic 對應詞 / pattern | 最權威 URL |
|---|---|---|
| **1. Legibility** | ACI（Agent-Computer Interface）+ Tool Documentation | [Building Effective Agents §Appendix](https://www.anthropic.com/engineering/building-effective-agents) |
| **2. System of Record** | CLAUDE.md hierarchy + Memory persistence | [Claude Code: How Claude remembers your project](https://code.claude.com/docs/en/memory) + [Multi-Agent Research System](https://www.anthropic.com/engineering/built-multi-agent-research-system) |
| **3. Progressive Disclosure** | **同詞**（Anthropic Skills 自己也用「core design principle」描述）| [Equipping Agents for the Real World with Agent Skills](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills) ⭐⭐⭐ |
| **4. Taste Invariants** | Evaluator-optimizer loops + Tool "poka-yoke"（如強制 absolute filepath）| [Building Effective Agents §Evaluator-optimizer](https://www.anthropic.com/engineering/building-effective-agents) |
| **5. Throughput Changes Merge Philosophy** | "Human evaluation catches what automation misses" + LLM-as-judge 並用 | [Multi-Agent Research System §Evaluation challenges](https://www.anthropic.com/engineering/built-multi-agent-research-system) |

**Anthropic 額外強調的 3 個 OpenAI 沒重點講的原則**：

| 原則 | 白話 | URL |
|---|---|---|
| **Simplicity** | 先用 basic LLM call、不要跳 multi-step agent | [Building Effective Agents §Simplicity](https://www.anthropic.com/engineering/building-effective-agents) |
| **Transparency** | "explicitly showing the agent's planning steps"——agent 自己秀 plan | [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) |
| **Memory persistence** | context 滿前先存外部、subagent 用 fresh context 接力 | [Multi-Agent Research System](https://www.anthropic.com/engineering/built-multi-agent-research-system) |

#### 推薦閱讀順序（45 + 20 min）

**先讀這 3 篇（總計 ~45 min）**：

1. [Anthropic — Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) ⭐⭐⭐ — 涵蓋原則 #1 + #4 + Simplicity / Transparency、**最基礎、先讀**
2. [Anthropic Engineering — Equipping Agents for the Real World with Agent Skills](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills) ⭐⭐⭐ — 涵蓋原則 #3、Anthropic 直接用 "progressive disclosure" 詞、3-tier loading 完整解說
3. [Claude Code — How Claude remembers your project](https://code.claude.com/docs/en/memory) ⭐⭐ — 涵蓋原則 #2、CLAUDE.md 4-tier hierarchy + `@-import` + AGENTS.md 互通

**再讀這 1 篇（~20 min）**：

4. [Anthropic — How we built our multi-agent research system](https://www.anthropic.com/engineering/built-multi-agent-research-system) — 補 #2 + #5 + Memory persistence production 案例

**OpenAI 原始文章**：

5. [OpenAI — Harness Engineering](https://openai.com/index/harness-engineering/) — Codex 自身 case study、5 原則的源頭

### 🛠️ 把這 5 原則套到 Skills + CLAUDE.md 設計（Stage 5 實作對照）

這 5 個原則**不是抽象的設計理論**——當你在 [Stage 5.3](05-claude-code-ecosystem.md#53--skillsclaude-code-的行為層-claude-code-生態最關鍵的一層) 寫自己的 SKILL.md、或 [Stage 5.1](05-claude-code-ecosystem.md#51--claude-code-基礎) 寫專案的 CLAUDE.md 時、直接套這 2 個 checklist：

#### 寫 SKILL.md 時的 checklist

| 原則 | Do（這樣做）| Don't（不要這樣）|
|---|---|---|
| **Legibility** | description 寫清楚「**何時觸發**」、tool param 用一致 schema 命名 | description 用模糊詞如「help with X」、tool param 命名混亂 |
| **Progressive Disclosure** | SKILL.md 短（< 200 行）、細節 / examples 放 `references/` 內、agent 需要才讀 | 把所有 example / edge case 都塞進 SKILL.md 主檔 |
| **System of Record** | `references/` 是 single source、SKILL.md 用 pointer 引、不重複 | `references/` 內容也 copy 一份到 SKILL.md「以防萬一」 |
| **Taste Invariants** | success criteria 寫死（「output 必須符合 X schema」「token < Y」）、用 YAML preset enforce | 「盡量好」「合理」這種主觀詞、靠 agent 自己 judge |
| **Throughput / Merge** | 附 acceptance check（lint / unit test / preset YAML）、不靠人逐字 review | 全靠操作者跑完讀結果再決定 |

#### 寫 CLAUDE.md 時的 checklist

| 原則 | Do | Don't |
|---|---|---|
| **Legibility** | 用 markdown header 分區、agent 能 scan；conventions 寫具體（"2-space indent"）| 一堆連續段落沒 anchor；「format code properly」這種空話 |
| **Progressive Disclosure** | CLAUDE.md 短（Anthropic 建議 **< 200 行**）、用 `@-import` 引外部、`.claude/rules/<topic>.md` 分主題切割 | 把所有 rules 塞同一個 CLAUDE.md 變 1000 行 |
| **System of Record** | CLAUDE.md 是 100 行 entry map、指向 `docs/` + `.coord/memory.yml`；用 `@AGENTS.md` import 共享 | architecture decision 同時寫 CLAUDE.md + docs/ 兩份 |
| **Taste Invariants** | "Always run `make lint` before commit"、"File < 500 lines" 這種**可驗證**規則 | "follow best practices"、"write clean code" |
| **Transparency** | 寫 "show plan before edit > 50 lines" 這種 explicit step 要求 | 預期 agent 默默做完不報告 |

→ **這 2 個 checklist 是 [Stage 5.1](05-claude-code-ecosystem.md#51--claude-code-基礎) + [Stage 5.3](05-claude-code-ecosystem.md#53--skillsclaude-code-的行為層-claude-code-生態最關鍵的一層) 設計時的直接 reference**——遇到「我這個 SKILL.md 該多長」「CLAUDE.md 怎麼分」這種具體問題、回來查這張表。

## 📖 完整 reading path（按 depth 分層）

按深度排序、不必全讀。Foundation tier 必讀（~95 min）、其他**遇到問題再深挖**：

**Foundation tier**（先讀這 4 個、~95 min 總長）:
- [Anthropic — Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents)
- [Cognition — Don't Build Multi-Agents](https://cognition.ai/blog/dont-build-multi-agents)
- [Anthropic — How we built our multi-agent research system](https://www.anthropic.com/engineering/built-multi-agent-research-system)
- [Lilian Weng — LLM Powered Autonomous Agents](https://lilianweng.github.io/posts/2023-06-23-agent/)

**Workflow patterns tier**:
- [LangGraph Planning Agents Tutorial](https://blog.langchain.com/planning-agents/)
- [Microsoft AutoGen docs](https://microsoft.github.io/autogen/)
- [DSPy](https://dspy.ai/learn/)

**Production / Harness tier**:
- [OpenAI Harness Engineering (2026-02)](https://openai.com/index/harness-engineering)
- [Hamel Husain Evals blog](https://hamel.dev/blog/posts/evals/)
- [Simon Willison coding agents notes](https://simonwillison.net/tags/coding-agents/)

**Frontier research papers**（挑 3-5 篇深讀）:
- ReAct / Reflexion / CoALA / Self-Discover / Voyager / Constitutional AI / AutoGen

**中文 / hands-on**:
- [李宏毅 GenAI 2024 / 2025](https://speech.ee.ntu.edu.tw/~hylee/)
- [datawhalechina/hello-agents](https://github.com/datawhalechina/hello-agents)

## ✅ 自我檢查

讀完本 stage 應該能：

- [ ] 用 **Types → Config → Repo → Service** 四層解釋 F11/F12 為什麼算 work boundary 違規
- [ ] 說出 12 個進階概念中 5 個的「在 stack 哪一層」+ 一句話定義
- [ ] 講得出 OpenAI 5 原則（Legibility / SoR / Progressive Disclosure / Taste Invariants / Throughput Changes Merge Philosophy）各自解什麼問題
- [ ] 知道下一步該翻哪份 paper / blog（不必全部讀完）
- [ ] 區分 PAR loop（單 agent 自我修正）vs agent-debate（2 agents 對峙）
- [ ] 在 brief 中明確寫出 task 的 work boundary（in / out）

→ 全部能做到 = 你已超出 Stage 7 production 化的範圍、進入 frontier agentic workflow design。**剩下就是看哪個 paper 對你最痛、深讀那個**。
